import MovieCard, { MovieCardSkeleton } from './MovieCard'

// SeriesCard is functionally identical to MovieCard but styled with a cyan accent.
// We import and export explicitly to avoid re-export syntax issues in some bundlers.
export { MovieCardSkeleton }
export default MovieCard
